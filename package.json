{
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "npx tsx index.ts",
    "db:push": "drizzle-kit push",
    "db:seed": "npx tsx src/seed.ts",
    "deploy": "npm run build && npm run db:push && npm run db:seed"
  },
  "dependencies": {
    "@fastify/static": "^8.1.0",
    "dotenv": "^16.5.0",
    "drizzle-orm": "^0.44.2",
    "fastify": "^5.7.4",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "drizzle-kit": "^0.31.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
